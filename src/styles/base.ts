import { css } from "lit";

export const style = css`
    *{
        font-family: inherit;
    }

    button{
        cursor: pointer;
        background: transparent;
        border: none;
        outline: none;
    }
`;

export default style;